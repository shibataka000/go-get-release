name: Test
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: golang:1.20
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: make test
        env:
          GOOS: linux
          GOARCH: amd64
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
